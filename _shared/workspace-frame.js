/**
 * WisdomLayers Reach - private workspace frame
 *
 * No-op. Used to render a teal border + "<name>'s Workspace, Private" badge
 * on every page as a visual cue that each contractor's data is siloed.
 * Removed per owner request - kept as a no-op rather than pulling the
 * applyWorkspaceFrame(name) call out of every page that calls it.
 */
function applyWorkspaceFrame(name) {}
